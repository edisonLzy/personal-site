import {User} from '../models/User';
import {UserInfo} from '../types'
export default {
    async sign(info:UserInfo){
    //  const ins = await User.create({
    //      'user_name':'leezhiyu'
    //  })
    //  console.log(ins)
    },
    login(){

    }
}